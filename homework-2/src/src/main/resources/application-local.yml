spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/tickets_local
    username: postgres
    password: postgres

  jpa:
    show-sql: true
    properties:
      hibernate:
        format_sql: true

logging:
  level:
    com.support.ticketsystem: DEBUG
    org.springframework.web: DEBUG
    org.hibernate.SQL: DEBUG
